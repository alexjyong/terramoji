{
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y wormhole && curl -LsSf https://astral.sh/uv/install.sh | sh && bash -c $(curl -fsSL https://qwen-code-assets.oss-cn-hangzhou.aliyuncs.com/installation/install-qwen.sh)"
}